import {BrowserRouter, Routes, Route, Navigate} from "react-router-dom";

const RotaPrivada = () => {
    const {Logado}
    }
    <Routes element = {<RotaPrivada />}>
    <Route path = "/pago" element = {<Pago />} />
    <Route path = "/pago/:username" element = {<Usuario />} />
    </Routes>