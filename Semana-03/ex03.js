numero = [1,2,3,4,5,6,7,8,9]
function funcao() {
    var calculo = numero % 2
    if (calculo == 0)
    {document.write("Este número é par")}
    else{ document.write("Este número é ímpar")}
}
console.log('Quantidade de números pares foi: ' + par + "E de ímpares foi: " + impar);